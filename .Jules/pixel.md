## 2026-02-04 - Hardcoded Brand Colors
**Learning:** Hardcoded brand colors (e.g. `#1a1a1a`) are scattered across components (ProductView, ProductCard), bypassing the design system tokens.
**Action:** Use `bg-brand-surface` and other tokens from the theme definition instead of hex codes.
