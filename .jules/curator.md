## 2025-02-12 - Component Grouping Strategy
Structure: Separating generic UI components from feature-specific components.
Rule: Components specific to a business feature (e.g., ProductCard, VariantSelector) must reside in a feature-named directory (e.g., components/store), not in generic UI directories (components/ui).
