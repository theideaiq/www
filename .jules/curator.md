## 2025-02-24 - Shared Package Autonomy
Structure: Shared packages (in `packages/`) must be self-contained units with their own tests and build scripts.
Rule: Tests for shared code must reside within the package directory, not in consuming applications.
