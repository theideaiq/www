# Curator's Journal

## 2024-05-22 - Layout Component Grouping
Structure: Group top-level layout components (Navbar, Footer, etc.) into `src/components/layout/`.
Rule: Components that are persistent across routes and define the page structure belong in `src/components/layout/`.
